const config = {
    currencies: ['RUB','USD','EUR'],
    declension: {
        stops: {
            zero: 'Без пересадок',
            one: 'Пересадка',
            some: 'Пересадки',
            many: 'Пересадок'
        }
    }
};

export default config;
