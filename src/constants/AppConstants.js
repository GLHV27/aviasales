import keyMirror from 'keymirror';

export default keyMirror({
    EXCHANGE_RATES_LOAD_SUCCESS: null,
    SET_CURRENT_CURRENCY: null,
    TICKETS_START_LOADING: null,
    TICKETS_SUCCESS: null,
    TICKETS_FAIL: null,
});
